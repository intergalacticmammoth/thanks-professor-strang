// unused for now, let's see if I need it.
export function UserMessage (
    name, email, password, bio, message
) {
    return {
        name: name,
        email: email,
        password: password,
        bio: bio,
        message: message
    };
}
